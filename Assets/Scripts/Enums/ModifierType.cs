/// <summary>
/// Тип модификатора стата (прямой, суммарный процент, процентный множитель).
/// </summary>
public enum ModifierType
{
    Flat = 300,
    PercentAdditive = 200,
    PercentMultiply = 100
}
